apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - serviceaccount.yaml
  - configmap.yaml
  - service.yaml
  - deployment.yaml
  - cronjob.yaml

# Production: add a sibling overlay (e.g. overlays/prod/) that patches configmap,
# references secret.yaml, and swaps emptyDir for a PVC on the data volume.
