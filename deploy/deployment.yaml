apiVersion: apps/v1
kind: Deployment
metadata:
  name: laas
  labels:
    app.kubernetes.io/name: laas
    app.kubernetes.io/component: ledger-service
spec:
  # Single-writer design: exactly one replica holds the write lock.
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: laas
  template:
    metadata:
      labels:
        app.kubernetes.io/name: laas
        app.kubernetes.io/component: ledger-service
    spec:
      serviceAccountName: laas
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: laas
          image: laas:latest
          ports:
            - containerPort: 8000
              protocol: TCP
              name: http
          envFrom:
            - configMapRef:
                name: laas-config
            - secretRef:
                name: laas-secrets
                optional: true
          volumeMounts:
            - name: signing-key
              mountPath: /var/run/secrets/laas/signing-key
              readOnly: true
            - name: data
              mountPath: /var/lib/laas/data
            - name: tmp
              mountPath: /tmp
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            failureThreshold: 12
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 10
          resources:
            requests:
              memory: "128Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: signing-key
          secret:
            secretName: laas-signing-key
            optional: true
        - name: data
          emptyDir: {}
        - name: tmp
          emptyDir: {}
